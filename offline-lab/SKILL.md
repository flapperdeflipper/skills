---
name: offline-lab
description: "The offline-lab project (air-gapped computing on low-power ARM: Buildroot OS, putter, disco, esp32-timesyncd, boxctl, bootconf) plus Buildroot/BR2_EXTERNAL package, defconfig and kernel conventions. Use when working in offline-lab, touching its OS image or signed packages, or writing Buildroot packages."
---

# offline-lab

Router. Read only the guide the task needs: `<guide>/GUIDE.md` in this skill's directory.

| Guide | Read when |
|---|---|
| `project` | Any work in `/share/syncthing/projects/offline-lab` or its components. Read before touching the OS image or signed packages |
| `buildroot` | Writing or reviewing Buildroot packages (`.mk`, `Config.in`), defconfigs or kernel fragments, in offline-lab or elsewhere |
