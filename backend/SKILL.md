---
name: backend
description: "Backend services: REST/GraphQL API design and OpenAPI specs, FastAPI with Pydantic v2 and async SQLAlchemy, PostgreSQL (EXPLAIN, indexes, JSONB, replication, VACUUM), and MCP servers/clients (tools, resources, transports, SDKs). Use for any of that design, build or tuning work."
---

# Backend

Router. Read only the guide the task needs: `<guide>/GUIDE.md` in this skill's directory. Each guide indexes its own `references/` (paths relative to the guide); read a reference only when the guide points to it for your task.

| Guide | Read when |
|---|---|
| `api-designer` | Designing REST or GraphQL APIs: resource modeling, versioning, pagination, errors, OpenAPI |
| `fastapi-expert` | FastAPI/Pydantic/async API work: endpoints, auth/JWT, async SQLAlchemy, WebSockets |
| `postgres-pro` | PostgreSQL schema, slow queries, EXPLAIN, JSONB, extensions, replication, VACUUM |
| `mcp-developer` | Building or debugging MCP servers/clients: handlers, resources, stdio/HTTP/SSE, schemas |

Language idioms live in the `languages` skill.
