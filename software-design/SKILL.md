---
name: software-design
description: "Designing code and domains: deep-module vocabulary (interfaces, seams, depth, testability), the project's domain model (CONTEXT.md glossary, ADRs), and throwaway prototypes to answer a logic or UI question. Use when designing or improving a module, discussing domain terminology, writing an ADR, or prototyping."
---

# Software design

Router. Read only the guide the task needs: `<guide>/GUIDE.md` in this skill's directory. Paths inside a guide are relative to that guide.

| Guide | Read when |
|---|---|
| `codebase-design` | Designing or improving a module's interface, finding deepening opportunities, placing a seam, making code testable or AI-navigable |
| `domain-modeling` | Discussing codebase terminology, writing or editing CONTEXT.md, recording an ADR |
| `prototype` | Sanity-checking a state model or logic, or exploring what a UI should look like, with throwaway code |

API contract design (REST/GraphQL, OpenAPI) is `api-designer` in the `backend` skill.
